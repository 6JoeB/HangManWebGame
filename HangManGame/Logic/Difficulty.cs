namespace HangManGame.Logic
{
    public enum Difficulty // enum = word + number <label> (type-safe, forces correct spelling/case etc) 
    {
        Easy = 15,
        Medium = 10,
        Hard = 5
    }
}